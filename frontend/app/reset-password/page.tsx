"use client";
import {FormEvent,useState} from "react";import Link from "next/link";import {AuthShell} from "@/components/AuthShell";import {Field,Notice} from "@/components/UI";import {api} from "@/lib/api";
export default function Reset(){const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [message,setMessage]=useState("");const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();try{const token=new URLSearchParams(location.search).get("token");const r=await api<{message:string}>("/auth/reset-password",{method:"POST",body:JSON.stringify({token,password,password_confirmation:confirm})});setMessage(r.message)}catch(e){setError((e as Error).message)}}return <AuthShell eyebrow="ACCOUNT RECOVERY" title="Choose a new password" subtitle="Resetting your password signs out every existing session."><form className="form" onSubmit={submit}>{message&&<Notice tone="success">{message}</Notice>}{error&&<Notice tone="error">{error}</Notice>}<Field label="New password" type="password" minLength={12} required value={password} onChange={e=>setPassword(e.target.value)}/><Field label="Confirm password" type="password" required value={confirm} onChange={e=>setConfirm(e.target.value)}/><button className="btn">Reset password</button><Link href="/login" className="text-link">Back to sign in</Link></form></AuthShell>}
