"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {AuthShell} from "@/components/AuthShell";import {Field,Notice} from "@/components/UI";import {api} from "@/lib/api";
export default function Register(){const [form,setForm]=useState({email:"",password:"",password_confirmation:""});const [message,setMessage]=useState("");const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");try{const r=await api<{message:string}>("/auth/register",{method:"POST",body:JSON.stringify(form)});setMessage(r.message)}catch(e){setError((e as Error).message)}}return <AuthShell eyebrow="JOIN GNKALGO" title="Create your account" subtitle="Start with a verified identity. Broker access remains completely separate."><form className="form" onSubmit={submit}>{message&&<Notice tone="success">{message}</Notice>}{error&&<Notice tone="error">{error}</Notice>}<Field label="Email address" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><Field label="Password" type="password" required minLength={12} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><Field label="Confirm password" type="password" required value={form.password_confirmation} onChange={e=>setForm({...form,password_confirmation:e.target.value})}/><p className="muted">Use 12+ characters with uppercase, lowercase, number and symbol.</p><button className="btn">Create secure account</button><div className="form-foot"><span>Already registered?</span><Link href="/login">Sign in</Link></div></form></AuthShell>}
