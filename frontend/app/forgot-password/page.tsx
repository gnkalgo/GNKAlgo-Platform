"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {AuthShell} from "@/components/AuthShell";import {Field,Notice} from "@/components/UI";import {api} from "@/lib/api";
export default function Forgot(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const r=await api<{message:string}>("/auth/forgot-password",{method:"POST",body:JSON.stringify({email})});setMessage(r.message)}return <AuthShell eyebrow="ACCOUNT RECOVERY" title="Reset your password" subtitle="We’ll send a short-lived recovery link if the account exists."><form className="form" onSubmit={submit}>{message&&<Notice tone="success">{message}</Notice>}<Field label="Email address" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn">Send recovery link</button><Link className="text-link" href="/login">Back to sign in</Link></form></AuthShell>}
